# AuditBee 2026-06-02 测试修复总结

## 一、今日工作概览

基于 2026-06-01 的 5 条链路优化（17 个文件，836 行新增），进行了完整的端到端测试、bug 修复和重新打包。

**6 个 commit，12 个文件改动，~200 行新增**

| Commit | 内容 |
|--------|------|
| `553c054` | feat: 5-chain optimization（6/1 提交） |
| `71c85b7` | fix: task_runner 'int' object has no attribute 'id' |
| `88f6974` | fix: KG build status reset uses valid JSON |
| `aadfdd4` | fix: LLM config persistence - empty model name |
| `88f6974` | fix: database.py pool_pre_ping + pool_recycle |
| `aadfdd4` | fix: frontend SettingsPage save logic |

---

## 二、发现的问题及修复

### 问题 1: task_runner.py `'int' object has no attribute 'id'`（Critical）

**状态**: ✅ 已修复

**现象**: 审计任务运行完成后报错 `AttributeError: 'int' object has no attribute 'id'`

**根因**: `task_runner.py:569` — `select(Finding.id)` 返回整数列表，代码又对整数调用 `.id`

```python
# 修复前（crash）
old_finding_ids = [f.id for f in (await db.execute(select(Finding.id).where(...))).scalars().all()]
# 修复后
old_finding_ids = list((await db.execute(select(Finding.id).where(...))).scalars().all())
```

**影响**: 阻断审计任务的数据安全写入

---

### 问题 2: KG build 状态卡死返回 409

**状态**: ✅ 已修复

**现象**: 上传法规文件后点击"构建"，返回 409 Conflict

**根因**: 进程被 Ctrl+C 中断时，DB 中 `building` 状态未重置。启动时的重置 SQL 把 `config_value` 设为字符串 `'false'` 而不是有效 JSON，导致 `json.loads('false')` 返回 `False`（布尔值），`.get()` 调用崩溃。

```python
# 修复前
_text("UPDATE configurations SET config_value = 'false' WHERE ...")
# 修复后
_text("UPDATE configurations SET config_value = :val WHERE ..."), {"val": '{"building": false, ...}'}
```

**影响**: 阻断知识图谱重建

---

### 问题 3: LLM 配置保存不持久（核心体验问题）

**状态**: ✅ 已修复

**现象**: 用户在设置界面填写 API Key 和模型名称，测试连接成功，保存后重启 exe，配置丢失。LightRAG 发送空模型名到 SiliconFlow → 400 "The parameter is invalid"。

**根因链路**:

```
.env: SILICONFLOW_MODEL= (空)
  ↓
前端加载: isPlaceholder("") → true → 自动填充 "deepseek-ai/DeepSeek-V3.2"
  ↓
setConfig(flat) + setDraft(flat) → 两者相同
  ↓
用户点击"保存": draft === config → "无配置变更" → 不发送到后端
  ↓
.env 中 SILICONFLOW_MODEL 始终为空
  ↓
LightRAG: config.get("model", "mimo-v2.5-pro") → "" (空字符串)
  ↓
SiliconFlow: model="" → 400 "The parameter is invalid"
```

**修复（3 个文件）**:

| 文件 | 修复 |
|------|------|
| `frontend/src/pages/SettingsPage.tsx` | 保存时，对已配置 API Key 的 provider，始终包含 model 和 base_url |
| `agent/tools/lightrag_tool.py` | 空模型名兜底：fallback 到 provider 默认模型 |
| `agent/config.py` | `get_llm_config()` 使用 `or` 处理空环境变量 |

**影响**: 阻断所有 LLM 相关功能（KG 构建、Agent Pipeline）

---

### 问题 4: Starlette 版本不兼容（exe 启动崩溃）

**状态**: ✅ 已修复（6/1）

**现象**: exe 启动即崩溃 `TypeError: Router.__init__() got an unexpected keyword argument 'on_startup'`

**根因**: FastAPI 0.109.0 与 Starlette 1.2.1 不兼容。Starlette 1.x 移除了 `on_startup` 参数。

**修复**: `backend/requirements.txt` 锁定 `starlette>=0.35.0,<0.36.0`

---

### 问题 5: Magika 模型未打包（PDF 转换失败）

**状态**: ✅ 已修复（6/1）

**现象**: KG 上传 PDF 时报错 `model dir not found at ...\magika\models\standard_v3_3`

**根因**: `build.spec` 缺少 `magika` 包的 `collect_all`

**修复**: 添加 `'magika'` 到 `build.spec` 的 `collect_all` 列表

---

### 问题 6: API 不返回 doc_metadata

**状态**: ✅ 已修复（6/1）

**现象**: 前端 Tooltip 错误详情永远不触发

**根因**: `documents.py` 的 `list_documents` 和 `get_document` 端点未包含 `doc_metadata` 字段

**修复**: 添加 `_parse_metadata()` 辅助函数，两个端点均返回 `doc_metadata`

---

### 问题 7: SQLAlchemy 连接池泄漏警告

**状态**: ✅ 已缓解

**现象**: 大量 `garbage collector is trying to clean up non-checked-in connection` 警告

**修复**: `database.py` 添加 `pool_pre_ping=True` + `pool_recycle=3600`

---

## 三、打包链路明确化

```
Step 1: npm run build                    → frontend/build/
Step 2: cp frontend/build → backend/static
Step 3: python -m PyInstaller build.spec → dist/AuditBee/_internal/
Step 4: mkdir dist/AuditBee/{config,data/*}
Step 5: cp .env.example → dist/AuditBee/config/.env
Step 6: cp model/ → dist/AuditBee/model/ (1.3GB)
Step 7: cp lightrag_output/ → dist/AuditBee/_internal/graphrag_index/lightrag_output/
Step 8: 验证关键文件（magika, lightrag, rapidocr, model, KG index）
```

**依赖清单**:

| 类别 | 包 | 数据文件 |
|------|---|---------|
| 文档处理 | pymupdf, mammoth, docx, olefile, markitdown, magika | magika model (3MB), rapidocr models (15MB) |
| LLM/Agent | langchain, langgraph, langchain_openai, langchain_anthropic | — |
| 知识图谱 | lightrag, sentence_transformers, transformers, tiktoken | embedding model (1.3GB) |
| 其他 | numpy, onnxruntime, lxml, bleach, reportlab | — |

---

## 四、E2E 测试结果

### 自动化测试（Python 脚本）

| 测试项 | 链路 | 结果 |
|--------|------|------|
| Health check | — | ✅ PASS |
| LLM 配置保存 | 链路4 | ✅ PASS |
| LLM 连接测试 | 链路4 | ✅ PASS (3672ms) |
| Placeholder 拒绝 | 链路4 | ✅ PASS (422) |
| 文档上传 | 链路1 | ✅ PASS |
| 文档处理 | 链路1 | ✅ PASS |
| Agent Pipeline | 链路2 | ✅ PASS (6 findings, report 4087 chars) |
| SSE 事件流 | 链路5 | ✅ PASS (agent_thinking + done) |
| 仪表盘统计 | — | ✅ PASS |
| KG 查询 | 链路3 | ✅ PASS |
| KG 构建 | 链路3 | ✅ PASS (0 errors) |

### 配置持久化测试

| 测试 | 结果 |
|------|------|
| .env 写入 SILICONFLOW_MODEL | ✅ `deepseek-ai/DeepSeek-V3.2` |
| LLM adapter 加载正确模型 | ✅ `siliconflow (model=deepseek-ai/DeepSeek-V3.2)` |
| KG 构建无 400 错误 | ✅ 0 errors |

---

## 五、当前状态

### 已发布到 GitHub 的 Commit

| Commit | 日期 | 内容 |
|--------|------|------|
| `553c054` | 6/1 | 5-chain optimization |
| `71c85b7` | 6/2 | task_runner id bug |
| `88f6974` | 6/2 | KG build status + DB pool |
| `aadfdd4` | 6/2 | Config persistence |

### dist/AuditBee/ 打包状态

| 检查项 | 状态 |
|--------|------|
| exe 启动 | ✅ |
| 前端页面 | ✅ |
| LLM 配置 | ✅ |
| 文档上传 | ✅ |
| KG 构建 | ✅ (0 errors) |
| Agent Pipeline | ✅ |
| SSE 事件流 | ✅ |
| 嵌入模型 (1.3GB) | ✅ |
| KG 索引 | ✅ |
| magika 模型 | ✅ |

### 已知限制

| 问题 | 影响 | 优先级 |
|------|------|--------|
| KG 构建慢（每文件 3-4 分钟） | 用户体验 | 低（外部 API 延迟） |
| PDF 扫描件 OCR 慢 | 大文件处理 | 低 |
| .doc 格式解析有限 | 旧格式支持 | 低 |
| 连接池泄漏警告（GC 清理） | 日志噪音 | 低（不影响功能） |

---

## 六、待办事项

### 高优先级
1. 用户手工测试验证（配置、文档、KG、Agent 全流程）
2. 确认 SiliconFlow API 延迟问题是否需要切换 Provider

### 中优先级
3. KG 构建进度反馈（前端显示当前正在处理哪个文件）
4. 飞书通知集成测试
5. 报告导出 PDF 测试

### 低优先级
6. 连接池泄漏根因修复（线程中 SQLAlchemy 会话管理）
7. KG 构建并发优化（减少单文件处理时间）
8. 前端错误提示国际化
