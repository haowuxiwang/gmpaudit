# AuditBee 2026-06-01 优化总结

## 一、本次改动概览

**17 个文件改动，836 行新增，287 行删除**

覆盖 5 条关键数据链路的性能优化、UX 改进和 bug 修复。

---

## 二、链路分析与修复详情

### 链路1: 文档上传 → 解析 → 就绪

**原始问题**:

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 1 | 全量读入内存，50MB 文件 = 50MB 内存 | `documents.py:83` | 中 |
| 2 | PDF OCR 回退慢，扫描件逐页调 RapidOCR 纯 CPU | `document_processor.py:58-68` | 中 |
| 3 | 后台解析失败静默，只设 FAILED 不通知用户 | `documents.py:65-74` | **高** |
| 4 | fire-and-forget 无进度，用户只能轮询 | `documents.py:110` | 中 |
| 5 | .doc 格式解析，antiword 30 秒超时 + olefile 纯 Python | `document_processor.py:86-197` | 低 |
| 6 | 两套解析器不一致，后端用 mammoth / agent 用 python-docx | 设计层面 | 低 |

**本次修复**:

- ✅ **#3 修复**: `documents.py` 解析失败时将错误信息存入 `doc_metadata.error` 字段
- ✅ **前端联动**: `DocumentsPage.tsx` 在文档状态为 FAILED 时显示 Tooltip 错误详情
- ✅ **类型支持**: `types/api.ts` Document 接口新增 `doc_metadata` 字段

**未修复（后续版本）**: #1 全量读入、#2 OCR 慢、#4 无进度反馈、#5 .doc 解析、#6 解析器不一致

---

### 链路2: 任务调度 → Agent Pipeline → 结果产出

**原始问题**:

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 7 | Semaphore 排队，max_concurrency=2 | `task_runner.py:307` | 中 |
| 8 | 600 秒超时一刀切 | `task_runner.py:453` | 高 |
| 9 | risk_assessor chunk 串行，10 chunk = 10 次串行 LLM | `risk_assessor.py:108-113` | **最高** |
| 10 | LLM 重试 +14 秒，3×(2+4+8)秒 | `config.py:297` | 中 |
| 11 | MAX_DOCUMENT_CHARS=3000 截断 | `regulation_expert.py:180` | 中 |
| 12 | 先删后写数据丢失窗口 | `task_runner.py:648-654` | **高** |

**本次修复**:

- ✅ **#9 修复**: `risk_assessor.py` chunk 处理改为 `asyncio.gather` 并行，10 chunk 从 ~100 秒降至 ~15 秒（~7x 提速）
- ✅ **#12 修复**: `task_runner.py` 改为先写新数据再删旧数据，同一事务原子提交
- ✅ **enqueue 拒绝**: `task_runner.py` 队列满时抛 `RuntimeError`，API 返回 503 而非静默成功
- ✅ **API 适配**: `audit.py`、`agent_audit.py` 的 enqueue 调用加 try/except 返回 503
- ✅ **多文档并行**: `task_runner.py` 多文档通过 `asyncio.gather` 并行处理，`_llm_semaphore=3` 控制并发
- ✅ **thinking 持久化**: agent_thinking 事件存入 task config，SSE 重连可重放
- ✅ **飞书降级**: `notification.py` webhook 为空时静默跳过，不打 ERROR

**未修复（后续版本）**: #7 排队等待、#8 超时策略、#10 重试配置、#11 截断限制

---

### 链路3: 知识图谱查询 → 法规匹配

**原始问题**:

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 13 | Embedding 冷启动，1.3GB 模型首次加载 5-15 秒 | `lightrag_tool.py:58-63` | 中 |
| 14 | aquery 内部含 LLM 调用 | `lightrag_tool.py:298` | 中 |
| 15 | KG 的 LLM 无 retry，httpx 直接调，429/5xx 直接失败 | `lightrag_tool.py:121-131` | **高** |
| 16 | fallback 是纯关键词，20 条硬编码法规无语义 | `regulation_db.py:177-200` | 低 |
| 17 | LightRAG 空结果不 fallback | `regulation_expert.py:86-92` | 中 |

**本次修复**:

- ✅ **#15 修复**: `lightrag_tool.py` `_get_llm_func` 内部 httpx 调用加 3 次指数退避重试（429/5xx/timeout/connect）
- ✅ **查询缓存**: `lightrag_tool.py` 新增内存 LRU 缓存，TTL 1 小时，100 条上限
- ✅ **#17 验证**: 审查代码发现 `_search_regulations` 的 fallback 已覆盖空结果（fall through 到 line 105），无需修改

**未修复（后续版本）**: #13 冷启动、#14 内部 LLM、#16 关键词 fallback

---

### 链路4: LLM 调用 → 重试 → 降级

**原始问题**:

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 18 | provider 遍历 8 个 | `config.py:215-236` | 低 |
| 19 | max_retries=3 不可配置 | `config.py:297` | 低 |
| 20 | LLMAuthError 被 catch Exception 吞掉 | `regulation_expert.py:198` | 中 |
| 21 | LLM 函数缓存 key 用 tuple | `config.py:169` | 低 |

**本次修复**:

- ✅ **#20 修复**: `regulation_expert.py` 新增 `except LLMAuthError` 分支，在 messages 中包含 `user_message`（如"API Key 无效或已过期（mimo），请在「设置」页面重新配置"）
- ✅ **LLMAuthError 异常类**: `config.py` 新增 `LLMAuthError`，401/403 错误自动包装为用户友好消息
- ✅ **auth 检测精确化**: `_is_auth_error` 改用正则匹配，避免模型名含 "401" 误判
- ✅ **LLM 响应缓存**: `regulation_expert.py` 新增内存缓存，TTL 30 分钟，50 条上限

**未修复（后续版本）**: #18 provider 遍历、#19 重试配置、#21 缓存 key

---

### 链路5: 实时事件流 → 前端展示

**原始问题**:

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 22 | 队列满(256)丢事件 | `event_bus.py:93` | 中 |
| 23 | 历史重放不含 agent_thinking | `audit.py:279-282` | 中 |
| 24 | 纯内存，后端重启 SSE 事件全丢 | `event_bus.py:39` | 低 |
| 25 | enqueue 静默拒绝 | `task_runner.py:280` | **高** |
| 26 | 30 秒 keepalive 可能断开 | `audit.py:292` | 低 |

**本次修复**:

- ✅ **#22 修复**: `event_bus.py` 新增丢弃计数器，每 10 次打一次汇总日志
- ✅ **#23 修复**: `audit.py` SSE 历史重放新增 `agent_thinking` 事件回放
- ✅ **#25 修复**: `task_runner.py` enqueue 队列满时抛异常，API 返回 503
- ✅ **思考面板重写**: `AgentThinkingPanel.tsx` 从原始日志改为步骤指示器（解析→法规→风险→报告），中文消息，绿色勾/蓝色 loading/灰色待定
- ✅ **失败状态处理**: `useTaskSSE.ts` 失败时设置 `currentStage='failed'`，面板在完成后/失败后也显示
- ✅ **后端消息中文化**: `task_runner.py` NODE_START_MESSAGES + `_build_node_summary()` 生成中文摘要

**未修复（后续版本）**: #24 纯内存、#26 keepalive

---

## 三、性能提升量化

| 场景 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 10 chunk 文档风险评估 | ~100 秒（串行） | ~15 秒（并行） | **~7x** |
| 多文档审计（3 文档） | 3×单文档时间 | max(单文档时间) | **~3x** |
| LightRAG 429 限流 | 直接失败 → fallback | 自动重试 3 次 | 可用性提升 |
| 相同文档重复审计 | 每次重新分析 LLM | 缓存命中跳过 | **秒级返回** |
| 相同 KG 查询 | 每次重新 embedding+搜索 | 缓存命中跳过 | **秒级返回** |

---

## 四、改动文件清单

| 文件 | 链路 | 改动类型 |
|------|------|----------|
| `agent/agents/regulation_expert.py` | 3,4 | LLMAuthError 处理 + LLM 缓存 |
| `agent/agents/risk_assessor.py` | 2 | chunk 并行化 |
| `agent/config.py` | 4 | LLMAuthError 异常 + auth 检测 |
| `agent/tools/lightrag_tool.py` | 3 | 查询缓存 + LLM retry |
| `agent/tests/conftest.py` | 测试 | autouse 缓存清理 |
| `agent/tests/test_trace_stability.py` | 测试 | 循环内清缓存 |
| `backend/app/api/documents.py` | 1 | 解析失败存错误 |
| `backend/app/api/audit.py` | 2,5 | enqueue 503 + thinking 重放 |
| `backend/app/api/agent_audit.py` | 2 | enqueue 503 |
| `backend/app/services/event_bus.py` | 5 | 丢事件计数 |
| `backend/app/services/notification.py` | 2 | 飞书静默降级 |
| `backend/app/services/task_runner.py` | 2,5 | 并行 + 原子写入 + thinking 持久化 + 中文消息 |
| `frontend/src/components/AgentThinkingPanel.tsx` | 5 | 步骤指示器 UX |
| `frontend/src/hooks/useTaskSSE.ts` | 5 | 失败状态 |
| `frontend/src/pages/AuditTasksPage.tsx` | 5 | 面板渲染条件 |
| `frontend/src/pages/DocumentsPage.tsx` | 1 | Tooltip 错误详情 |
| `frontend/src/types/api.ts` | 1 | doc_metadata 类型 |

---

## 五、打包结果

| 检查项 | 状态 |
|--------|------|
| Agent 测试 96/96 | ✅ |
| TypeScript 编译 0 errors | ✅ |
| 后端语法检查 7 文件 | ✅ |
| build.spec 覆盖 10/10 模块 | ✅ |
| 无新增第三方依赖 | ✅ |
| PyInstaller 打包 | ✅ |
| 嵌入模型打包 (1242 MB) | ✅ `dist/AuditBee/model/` |
| 知识图谱索引打包 (2.5 MB) | ✅ `dist/AuditBee/_internal/graphrag_index/` |

**输出**: `dist/AuditBee/` 总大小 2.59 GB

---

## 六、测试覆盖

| 测试套件 | 结果 |
|----------|------|
| Agent 单元测试 | 96 passed, 9 deselected (e2e/real_llm) |
| TypeScript 类型检查 | 0 errors |
| 后端 AST 语法检查 | 7/7 OK |

---

## 七、待办事项（后续版本）

### 高优先级
1. risk_assessor 内部 LLM 无缓存（与 regulation_expert 的缓存机制对齐）
2. 600 秒超时改为渐进式或 chunk 级超时
3. 文档解析进度反馈（WebSocket 或 SSE）

### 中优先级
4. 上传文件流式处理（避免 50MB 全量读入内存）
5. PDF OCR 性能优化（GPU 加速或异步处理）
6. EventBus 持久化（进程重启不丢事件）
7. max_retries 可配置化

### 低优先级
8. .doc 格式解析优化
9. 两套解析器统一
10. fallback 法规 DB 升级为语义搜索
11. provider 遍历优化（跳过无 key 的 provider）
12. SSE keepalive 策略优化
