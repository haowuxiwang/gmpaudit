# AuditBee 当前发布基线审查报告

审查日期：2026-06-18

审查范围：当前代码仓库的前端、后端、RAG、KG、LLM、agent 链路、依赖完整性、版本兼容性、功能完整性、性能、安全性、错误处理、可观测性、文档准确性、pipeline 是否堵塞，以及当前是否适合面向无代码用户以 zip 形式分发。

约束：本次只做审查和报告，不修改代码、不清理 dist、不重打包、不执行破坏性命令。用户明确当前没有服务器和域名，目标是本地 zip 分发，且无需登录。

## 结论

当前代码“核心审计链路”有可运行基础，但**当前不建议作为 zip 面向无代码用户分发**。

原因不是缺登录，登录确实不需要；真正阻断是：

- 现有 `dist\AuditBee` 包含疑似真实 API Key 和运行期数据。
- 分发验收脚本失败，当前 dist 包不能证明可发布。
- 用户可见中文文案/文档存在大量乱码。
- 干净环境依赖安装未验证。
- 部分路径/打包验收逻辑仍不稳。

我没有修改仓库代码。已做只读/非代码变更验证：

- `python -m py_compile backend\app\main.py backend\app\launcher.py backend\app\tkinter_launcher.py agent\graph.py agent\config.py agent\tools\lightrag_tool.py`：通过。
- `python scripts\verify_pipeline.py --quick`：13/13 通过，但暴露 report 写盘路径权限错误。
- `python -m pytest agent\tests\test_trace_layer1_kg.py agent\tests\test_trace_layer3_rag.py agent\tests\test_graph_unit.py -q`：32 通过。
- `python -m pytest backend\tests\test_api_config.py backend\tests\test_api_kg.py backend\tests\test_task_runner.py -q`：97 通过。
- `npx.cmd tsc --noEmit`：通过。
- `python scripts\test_dist.py --port 8765`：21 项中 12 项失败，当前分发包不可作为发布证据。

## 当前是否支持分发

**不支持直接分发当前 zip/dist 包。**

源码层面已有发布雏形，核心链路也有部分验证通过，但现有分发物不满足面向无代码用户发布的最低条件。尤其是 `dist\AuditBee` 当前存在敏感配置、历史运行数据、验收失败和乱码问题，不能作为可信发布包发给外部用户。

如果强行分发，主要风险是：

- 用户解压后看到乱码，无法理解启动器、设置说明、错误提示和文档。
- 可能把开发环境中的 API Key、数据库、已上传文档、历史审计记忆一起发出去。
- 用户首启后 API 调用、上传、任务运行、KG 查询、报告生成未被当前验收脚本证明可用。
- 干净机器上依赖、模型、LightRAG、OCR、PyInstaller 打包资源可能出现缺包或版本冲突。

## 已具备的分发基础

- 本地桌面/浏览器形态已经成型：PyInstaller exe、tkinter 启动器、FastAPI 静态前端、SQLite、本地模型和 LightRAG 预构建索引都有设计支撑。
- 后端有基础生产防护：日志轮转、健康检查、session token、生产模式限流、基础 metrics。
- RAG/KG 快速验证通过，LightRAG 输入/输出、fallback DB、trace 路径可用。
- agent 链路是顺序 LangGraph：`parse_doc -> regulation_expert -> risk_assessor -> report_writer`，不是分布式多 agent；这对 zip 单机分发反而更合适。
- 打包脚本已有安全意识：临时替换 `.env`、复制前端、模型、LightRAG、关键依赖，并检查 RapidOCR/Magika/LightRAG。
- 无需登录的产品方向基本合理。当前生产模式有 session token，但这是本地前端访问后端的运行期 token，不是用户登录系统。

## 发布阻断问题

### 1. 当前 dist 包泄露配置风险

`dist\AuditBee\config\.env` 被分发测试判定含真实 key。我只确认到敏感键名：

- `SILICONFLOW_API_KEY`

我没有输出密钥值。

这必须作为 P0 阻断。发布前必须重新生成干净包，确保：

- `dist\AuditBee\config\.env` 不含任何真实 API Key。
- `_internal\config` 不含 `.env.bak`、真实 `.env` 或开发机配置。
- 分发包内不存在任何用户或开发者私有密钥。

相关验证结果：

```text
python scripts\test_dist.py --port 8765
1.3 No real API keys in .env: FAIL
```

### 2. 当前 dist 包带运行期数据

`dist\AuditBee\data` 内已有运行期文件，包括：

- `audit_memory.jsonl`
- `database\gmp_audit.db`
- `database\gmp_audit.db-shm`
- `database\gmp_audit.db-wal`
- `documents\...` 已上传文档
- `kg_input\...` 首次运行复制出的法规输入

对 zip 给外部无代码用户来说，这会把开发/测试状态一起发出去，属于必须清理并用“首次运行初始化”的问题。

发布包应该只保留：

- exe
- `_internal` 必需运行资源
- 模型
- 预构建法规 KG 资源
- 干净配置模板
- 空的运行目录骨架

不应该包含：

- 开发数据库
- 历史上传文档
- 审计报告
- audit memory
- WAL/SHM
- 任何本地测试输出

### 3. 分发验收失败

当前 `scripts/test_dist.py` 未适配生产模式 session token，大量 API 返回 401。

实际结果：

```text
Total: 21 | Passed: 9 | Failed: 12
```

失败项包括：

- `1.3 No real API keys in .env`
- `2.4 Document list -- status=401`
- `2.5 Task list -- status=401`
- `2.6 Report list -- status=401`
- `2.7 Config list -- status=401`
- `2.8 Alert list -- status=401`
- `2.9 KG status -- status=401`
- `2.10 Dashboard -- status=401`
- `3.1 Document upload -- status=None`
- `4.1 KG status query`
- `4.2 KG documents list`
- `4.3 KG query -- status=401`

这说明当前验收脚本不能证明分发包可用。需要更新测试脚本先调用 `/api/auth/token`，后续请求带 `Authorization: Bearer <token>`。

### 4. 用户可见中文大量乱码

README、ARCHITECTURE、`.env.example`、launcher 文案、错误提示、报告模板、测试样本文本均出现 mojibake。

示例表现：

- `鍚姩 AuditBee`
- `鏂囨。`
- `鐭ヨ瘑鍥捐氨`
- `璇锋眰瓒呮椂`

这不是单纯终端显示问题。多份源文件内容本身已经是乱码形态。无代码用户会直接认为产品损坏。

发布前至少必须修复：

- `README.md`
- `ARCHITECTURE.md`
- `config\.env.example`
- `backend\app\tkinter_launcher.py` 用户界面文案
- `frontend\src\services\api.ts` 错误提示
- `backend\app\api\*.py` 用户可见错误提示
- `agent\agents\report_writer.py` 报告模板
- `backend\app\services\task_runner.py` 任务事件文案

### 5. 依赖完整性未达发布级

发现的风险信号：

- 本机 `pip show` 显示 `langchain-anthropic` 未安装。
- 但 `scripts\build.spec` 中收集了 `langchain_anthropic`。
- `backend\requirements.txt` 中 `langchain-anthropic` 被注释为 optional。
- `agent\requirements.txt` 中又要求 `langchain-anthropic>=0.3.0`。
- `backend\requirements.txt` 锁定 `langchain==0.2.17`，同时锁定 `langchain-openai==1.2.1`、`langgraph==1.1.10`，属于 LangChain 生态跨代混搭，需要干净环境安装验证。

本机环境能导入或测试通过，不代表干净机器能按 requirements 成功安装并打包。

发布前必须做：

- 新建干净 Python 3.11 虚拟环境。
- `pip install -r backend\requirements.txt`
- `pip check`
- 验证 `python -c "import langchain_openai, langgraph, lightrag, sentence_transformers"`
- 如果支持 Anthropic，就明确把依赖纳入 backend requirements；如果不支持，就从 provider、UI、build spec 中移除或标注不可用。

### 6. report_writer 路径隔离有问题

快速验证中出现：

```text
Report Writer: failed to save report: [Errno 13] Permission denied: 'D:\learn\claudecode\data\reports\test_20260618_093132.md'
```

这说明某些 agent standalone/fallback 路径会写到：

```text
D:\learn\claudecode\data\reports
```

而不是当前仓库：

```text
D:\learn\claudecode\gmpaudit\data\reports
```

源头在 `agent\agents\report_writer.py` fallback 路径：

```python
report_dir = Path(__file__).resolve().parent.parent.parent.parent / "data" / "reports"
```

这对 zip 分发和数据隔离都是风险。发布前应统一所有 agent/backend 路径解析，以 `backend.app.core.paths` 或明确的 app dir/data dir 为唯一来源。

### 7. 无代码首启体验未被验收

当前没有通过完整证据链证明：

1. 全新机器解压 zip。
2. 双击 `AuditBee.exe`。
3. launcher 正常显示中文。
4. 用户选择 provider。
5. 用户填 API Key。
6. 连接测试成功。
7. 系统自动打开浏览器。
8. 上传真实文档。
9. 文档处理成功。
10. 创建审计任务。
11. agent 执行并实时展示 thinking/progress。
12. 生成 findings。
13. 生成报告。
14. 导出报告。
15. 关闭后重启仍能读到配置和历史数据。

当前 `scripts/test_dist.py` 试图覆盖部分流程，但因为 token 和 dist 污染问题失败，不能作为发布证据。

### 8. AI 质量仍是 v1 风险

这些可作为下一版本问题，但发布说明必须诚实标注：

- 没有 verification agent/self-check loop。
- 没有专门挑战 findings 的 agent。
- 长文档默认只给 LLM 3000 chars，可能漏上下文。
- fallback regulation DB 规模有限。
- 审计输出质量高度依赖用户配置的 LLM provider、model、API key 和网络质量。

### 9. 文档准确性不足

文档声称系统是“多 Agent + LightRAG 知识图谱”，但实际 agent 是 deterministic sequential pipeline，不是分布式多 agent，也不是并行自治 agent team。

这不一定是坏事。对本地 zip 分发，顺序 pipeline 更稳定。但文档应该准确表达：

- 当前是“顺序多节点 agent pipeline”。
- 不是服务器集群。
- 不是多进程/多机分布式。
- 无需登录。
- 需要至少一个 LLM API Key 才能跑完整智能审计。

## pipeline 是否有堵塞

从当前只读验证看，源码层 pipeline 快速检查没有堵塞：

- KG 数据层通过。
- LLM fallback 创建通过。
- RAG/KG trace 通过。
- LangGraph 4 节点 trace 通过。
- 选定 agent/backend tests 通过。
- 前端 TypeScript 通过。

但面向分发的 pipeline 有堵塞：

- dist 验收脚本失败。
- 生产 token 未被验收脚本处理。
- 当前 dist 包不是干净包。
- 上传业务流在 dist 验收中失败。
- 首启 GUI 中文乱码未验证修复。
- 真实 LLM E2E 没有本次通过证据。

## 性能风险

- embedding 模型约 1.3GB，首次加载可能慢。
- LightRAG preload 是后台非阻塞，但首次查询可能仍有冷启动。
- RAG 查询 timeout 为 120s，用户可能感知为卡住。
- batch 文档处理有并发，但 SQLite + LLM 并发 + 本地模型加载在低配机器上可能不稳。
- `MAX_CONCURRENT_TASKS=5` 对普通无代码用户本地机器可能偏高，需要发布前压测低配机器。
- 大文档只给部分内容进 LLM，性能上省钱但质量有折中。

## 安全性风险

已具备：

- 本地生产模式 session token。
- 生产模式限流。
- 上传文件大小限制。
- 文件名 path traversal 检查。
- 配置 API 会 mask key。
- `.env` 写入有原子写逻辑。
- 全局 exception handler 避免 traceback 直接泄漏给用户。

仍需修复：

- 当前 dist 包疑似包含真实 key。
- 当前 dist 包包含运行期数据库和上传文档。
- `/api/auth/token` 对本机所有访问者可取 token；在无登录本地产品中可以接受，但文档要说明只绑定 `127.0.0.1`，不要暴露到局域网。
- `launcher.py` 使用 `shell=True` 打开浏览器，当前命令参数固定，风险较低，但仍建议后续收敛。
- `.env.example` 乱码会导致用户误填配置。

## 错误处理风险

已具备：

- LLM retry 和 auth error 包装。
- LightRAG 失败 fallback 到 regulation DB。
- report_writer LLM 失败 fallback 到模板报告。
- document processor 对失败状态有持久化。
- TaskRunner 对单文档错误、全部失败、取消、重启恢复有处理。

仍需修复：

- 用户可见错误提示大量乱码。
- fallback 报告模板乱码。
- 部分异常信息只记录通用“失败”，无代码用户难以自助处理。
- dist 验收业务流上传失败时只看到连接中止，缺少明确产品层错误。

## 可观测性风险

已具备：

- JSON rotating file log。
- request latency 和 status metrics。
- EventBus/SSE。
- task execution metadata。
- agent trace：KG/LLM/node events。
- `scripts\verify_pipeline.py` 分层检查。

仍需修复：

- `scripts\test_dist.py` 不适配生产 token，导致分发验收不可用。
- metrics 是内存态，重启丢失；本地 zip 可以接受。
- trace/report/任务日志如果乱码，会降低诊断价值。
- 没有一键导出诊断包给用户反馈问题。

## 可放入下一版本的问题

这些不应阻塞 v1 zip 本地分发，但建议进入下一个版本：

- 真正的多 agent 并行/分布式协作。
- verification agent、质量驱动重试、空 findings 自检。
- 更大的法规库和 FDA/EU/WHO 扩展。
- 更完整的观测面板、trace 可视化、成本统计。
- Docker/服务器部署、域名、登录、多用户权限。
- 更细的性能优化，例如 embedding 冷启动、RAG rerank、批量文档并发调度。
- Human-in-the-loop 审计复核节点。
- 更完整的报告质量评分和证据引用链。

## 达到可分发的最低通过条件

发布前必须完成以下事项：

1. 重新生成干净 `dist\AuditBee`

   - 无真实 key。
   - 无历史数据库。
   - 无上传文档。
   - 无 audit memory。
   - 只含 exe、模型、预构建法规 KG、空运行目录和干净 `.env.example`。

2. 修复所有用户可见乱码

   至少覆盖：

   - launcher
   - 设置页
   - 错误提示
   - README
   - `.env.example`
   - 报告模板
   - 任务进度文案

3. 更新 `scripts\test_dist.py`

   - 先调用 `/api/auth/token`。
   - 后续请求带 `Authorization: Bearer <token>`。
   - 覆盖启动、健康、首页、配置、KG 状态、上传、处理、创建任务、运行任务、报告生成、导出。

4. 干净环境验证依赖

   - Python 3.11 fresh venv。
   - `pip install -r backend\requirements.txt`
   - `pip check`
   - frontend `npm ci` 或 `npm install`
   - `npm run build`
   - PyInstaller build。

5. 完整首启验收

   - 解压 zip。
   - 双击 exe。
   - launcher 中文正常。
   - 填 API key。
   - 测试连接。
   - 上传样本文档。
   - 运行审计。
   - 查看 findings。
   - 生成报告。
   - 导出报告。
   - 重启后数据仍可用。

6. 发布文档准确说明

   - 无需登录。
   - 无服务器、无域名。
   - 用户只需解压、双击 exe、选择 LLM provider、填 API key。
   - 没有 API key 时不能完成完整智能审计，只能看到降级/模板能力。
   - 当前是本地单机顺序 agent pipeline，不是分布式多 agent 系统。

## 验证命令与结果摘要

### Python 编译检查

命令：

```powershell
python -m py_compile backend\app\main.py backend\app\launcher.py backend\app\tkinter_launcher.py agent\graph.py agent\config.py agent\tools\lightrag_tool.py
```

结果：通过。

### pipeline quick 验证

命令：

```powershell
python scripts\verify_pipeline.py --quick
```

结果：

```text
Layer 1: KG Data (7/7)
Layer 2: LLM Stability (2/2)
Layer 3: RAG/KG Retrieval (2/2)
Layer 5: LangGraph Path (2/2)
FINAL RESULT: 13/13 passed, 0 failed
```

同时出现：

```text
Report Writer: failed to save report: [Errno 13] Permission denied: 'D:\learn\claudecode\data\reports\test_20260618_093132.md'
```

### agent 选定测试

命令：

```powershell
python -m pytest agent\tests\test_trace_layer1_kg.py agent\tests\test_trace_layer3_rag.py agent\tests\test_graph_unit.py -q
```

结果：32 passed。

### backend 选定测试

命令：

```powershell
python -m pytest backend\tests\test_api_config.py backend\tests\test_api_kg.py backend\tests\test_task_runner.py -q
```

结果：97 passed。

### frontend TypeScript

命令：

```powershell
npx.cmd tsc --noEmit
```

结果：通过。

### dist 分发验收

命令：

```powershell
python scripts\test_dist.py --port 8765
```

结果：

```text
Total: 21 | Passed: 9 | Failed: 12
```

失败说明当前分发包不可作为发布证据。

## 当前 dist 风险

当前分发目录：

```text
D:\learn\claudecode\gmpaudit\dist\AuditBee
```

当前 exe 存在：

```text
D:\learn\claudecode\gmpaudit\dist\AuditBee\AuditBee.exe
```

检测到 exe 大小约：

```text
142,706,949 bytes
```

当前模型存在：

```text
D:\learn\claudecode\gmpaudit\dist\AuditBee\model\pytorch_model.bin
```

模型大小约：

```text
1,302,220,525 bytes
```

但当前 dist 不能直接发，因为：

- `config\.env` 含敏感配置风险。
- `data` 含运行期数据库和历史文档。
- 分发验收失败。

## 建议的 v1 发布定义

v1 可以定义为：

“面向无代码用户的本地 zip 分发 AI GMP 审计助手。用户解压后双击 exe，通过启动器选择 LLM provider 并填写 API key，在本地浏览器中上传 GMP 相关文档，系统使用本地法规知识图谱和顺序 agent pipeline 生成 findings 与审计报告。无需服务器、域名和登录。”

v1 不应承诺：

- 分布式多 agent。
- 无 API key 完整智能审计。
- 多用户协作。
- 企业级服务器部署。
- 100% 自动审计准确性。

## 最终判断

当前适合作为“发布候选前的工程基线”，不适合作为“可直接发给无代码用户的 zip 包”。

当以下三项同时完成后，才可以进入可分发状态：

1. 干净 dist 包生成并通过分发验收。
2. 用户可见乱码全部修复。
3. 首启到报告导出的真实 E2E 通过并记录证据。

